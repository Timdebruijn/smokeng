---
# Go's names for the architectures the release publishes, keyed by the fact.
_smokeng_arch_map:
  x86_64: amd64
  aarch64: arm64
  armv7l: arm
  i386: "386"
smokeng_arch: "{{ _smokeng_arch_map[ansible_facts['architecture']] | default('amd64') }}"
